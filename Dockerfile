# Використовуємо Node.js як базовий образ
FROM node:14

# Встановлюємо робочу директорію в контейнері
WORKDIR /app

# Копіюємо package.json та package-lock.json для інсталяції залежностей
COPY package*.json ./

# Інсталюємо залежності
RUN npm install

# Копіюємо решту файлів проекту
COPY . .

# Встановлюємо змінні середовища
ENV NODE_ENV=production

# Порт, на якому додаток буде працювати всередині контейнера
EXPOSE 3000

# Команда для запуску серверу
CMD ["node", "server.js"]
